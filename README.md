# LedgerLite

A tiny but production-minded money-transfer API demonstrating **idempotency**, **serializable transactions**, and **audit logging**.

## Run
```bash
make run
# then in another shell
curl -s localhost:8080/healthz
